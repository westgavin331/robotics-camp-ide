node_modules
dist
dist-ssr
*.local
.DS_Store
npm-debug.log*

# mkcert-generated local dev certs (private key material -- never commit)
*.pem

# Local env vars (MONGODB_URI etc) -- see backend/.env.example
.env
